#macro DOTDAE_OUTPUT_DEBUG         true    //Outputs extra debug info (this is useful to check the library is working properly!)
#macro DOTDAE_OUTPUT_LOAD_TIME     true    //Outputs the amount of time taken to load a .dae file to the console
#macro DOTDAE_DEFAULT_DIFFUSE_RGB  c_white //Default colour to embed in vertex buffers when no vertex colour is found for the model