/// @param UIThing

var thing = argument0;
var animation = thing.root.active_animation;

if (animation) {
    show_message("at some point in the future I'd like to create a tool to properly preview animations, but i don't have the time currently");
}