/// @param Dialog

var dialog = argument0;

// there were going to be other things in here but i can't remember what they were
// going to be, so now there's nothing and this just adds an unneeded script call
dr_box(dialog);