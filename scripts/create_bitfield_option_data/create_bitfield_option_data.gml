function create_bitfield_option_data(value, render, onvaluechange, text, sprite, index, width, height, x = 0, y = 0, color_active = c_ui_active_bitfield, color_inactive = c_white) {
    // @gml update lightweight objects
    return [value, render, onvaluechange, text, sprite, index, width, height, x, y, color_active, color_inactive];
}