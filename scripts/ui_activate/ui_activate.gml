/// @param UIThing
function ui_activate(argument0) {
    // this will most likely be used mainly for input dialogs but can be used
    // for other UIThings also

    var thing = argument0;

    Stuff.mode.ui.active_element = thing;


}
