/// @description uivc_bitfield_tile_passability_solid(UIThing);
/// @param UIThing

// value^0 won't do what i want it to do
(get_active_tileset()).passage[# Stuff.map.selection_fill_tile_x, Stuff.map.selection_fill_tile_y]=0;
