/// @description void dmu_data_dereference(UIThing);
/// @param UIThing

dialog_create_yes_or_no(argument0.root, "You want to dereference this map? (It won't be deleted from your computer; if you want it later, you can load and save it again)", dc_map_dereference);
