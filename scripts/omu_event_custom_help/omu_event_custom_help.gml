/// @param UIButton

var button = argument0;

dialog_create_notice(noone, "Sooner or later, you will probably want to be able to use the custom data types you create in a conversation event of some sort. Since data types are not known to the user and only by the editor, so must be the events that make use of them.\n\n(The real reason this button is here is because I don't have anything else to put in the space in the UI tab, though.)", "about", "okay", 400, 480);