/// @description  MenuMenu create_menu(text, width, height, root);
/// @param text
/// @param  width
/// @param  height
/// @param  root

with (instantiate(MenuMenu)){
    text=argument0;
    width=argument1;
    height=argument2;
    root=argument3;
    
    return id;
}
