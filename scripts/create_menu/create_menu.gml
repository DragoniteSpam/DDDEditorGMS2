/// @param text
/// @param width
/// @param height
/// @param root

with (instance_create_depth(0, 0, 0, MenuMenu)) {
    text = argument0;
    width = argument1;
    height = argument2;
    root = argument3;
    
    return id;
}