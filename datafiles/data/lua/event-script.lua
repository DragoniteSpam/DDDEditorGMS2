-- Required; the entrypoint for the code to be executed
-- @param entity the calling entity; you may want this
-- @param invoker the entity who invoked the event; this is usually the player
function ExecuteMe(entity, invoker)
    
end

 -- you may find it useful to create helper functions, but the only one you
 -- need is ExecuteMe